module.exports = {
    dist: {
        src: "<%= app.dirs.js %>/ui.js",
        dest: "<%= app.dirs.compiled %>/assets/js/ui.js",
        options: {
            // see below for options. this is optional. 
        }
    }
};